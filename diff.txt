diff --git a/src/app/(tabs)/_layout.tsx b/src/app/(tabs)/_layout.tsx
index b4991a1..8305922 100644
--- a/src/app/(tabs)/_layout.tsx
+++ b/src/app/(tabs)/_layout.tsx
@@ -1,4 +1,4 @@
-import { ErrorBoundary, triggerGlobalCrash } from "@/components/ErrorBoundary";
+import { ErrorBoundary } from "@/components/ErrorBoundary";
 import { ExploreMenuModal } from "@/components/modals/ExploreMenuModal";
 import { NotificationPermissionModal } from "@/components/modals/NotificationPermissionModal";
 import { SmartAppBanner } from "@/components/SmartAppBanner";
@@ -11,7 +11,12 @@ import { clampedScrollY } from "@/utils/scrollState";
 import { Ionicons } from "@expo/vector-icons";
 import { BottomTabBar } from "@react-navigation/bottom-tabs";
 import { useFonts } from "expo-font";
-import * as Notifications from "expo-notifications";
+let Notifications: any = null;
+try {
+  Notifications = require("expo-notifications");
+} catch (e) {
+  console.warn("Notifications disabled", e);
+}
 import { Tabs, useLocalSearchParams, usePathname } from "expo-router";
 import * as SplashScreen from "expo-splash-screen";
 import { StatusBar as ExpoStatusBar } from "expo-status-bar";
@@ -33,62 +38,12 @@ import {
   SafeAreaView,
   useSafeAreaInsets,
 } from "react-native-safe-area-context";
+import { signOut } from "firebase/auth";
+import { auth } from "@/config/firebase";
 
-// Prevent the splash screen from auto-hiding before asset loading is complete.
-SplashScreen.preventAutoHideAsync().catch(() => {});
-
-// Register global JS exception handler
-const globalObj = typeof global !== "undefined" ? global : window;
-const ErrorUtils = (globalObj as any).ErrorUtils;
-if (ErrorUtils) {
-  const defaultHandler = ErrorUtils.getGlobalHandler();
-  ErrorUtils.setGlobalHandler((error: any, isFatal?: boolean) => {
-    if (__DEV__) {
-      console.error("[Global JS Exception]", error, isFatal);
-    }
-    // Only trigger the local crash screen if the error is actually fatal
-    if (isFatal && triggerGlobalCrash) {
-      triggerGlobalCrash(
-        error instanceof Error ? error : new Error(String(error))
-      );
-      return;
-    }
-    if (defaultHandler) {
-      defaultHandler(error, isFatal);
-    }
-  });
-}
-
-// Register unhandled promise rejection handler
-try {
-  const tracking = require("promise/setimmediate/rejection-tracking");
-  tracking.enable({
-    all: true,
-    onUnhandled: (id: any, error: any) => {
-      if (__DEV__) {
-        console.warn("[Unhandled Promise Rejection]", error);
-      }
-      // Note: Do NOT trigger global crash screen for unhandled promise rejections
-      // as they are typically non-fatal background network operations (e.g. sync failures)
-    },
-  });
-} catch (e) {
-  if (__DEV__) {
-    console.warn("Could not register promise rejection tracker:", e);
-  }
-}
-
-if (Platform.OS !== "web") {
-  Notifications.setNotificationHandler({
-    handleNotification: async () => ({
-      shouldShowAlert: true,
-      shouldPlaySound: true,
-      shouldSetBadge: false,
-      shouldShowBanner: true,
-      shouldShowList: true,
-    }),
-  });
-}
+// NOTE: All module-level one-time setup (SplashScreen guard, ErrorUtils handler,
+// promise rejection tracker, notification display handler) now lives exclusively
+// in app/_layout.tsx to prevent duplicate registrations. Do NOT add them back here.
 
 function ToastNotification() {
   const toast = useAppStore((state) => state.toast);
@@ -325,6 +280,7 @@ function RootLayoutComponent() {
 
     // Check if app was opened from a notification while killed
     const checkKilledStateNotification = async () => {
+      if (!Notifications) return;
       try {
         const response = await Notifications.getLastNotificationResponseAsync();
         if (response && response.notification.request.content.data) {
@@ -340,8 +296,8 @@ function RootLayoutComponent() {
 
     checkKilledStateNotification();
 
-    const subscription = Notifications.addNotificationResponseReceivedListener(
-      (response) => {
+    const subscription = Notifications?.addNotificationResponseReceivedListener?.(
+      (response: any) => {
         const data = response.notification.request.content.data;
         if (data && data.url) {
           if (fontsLoaded && storeHydrated) {
@@ -354,17 +310,20 @@ function RootLayoutComponent() {
     );
 
     const foregroundSubscription =
-      Notifications.addNotificationReceivedListener((notification) => {
-        // Handle foreground notifications here if needed (e.g., updating badge counts or local state)
-        console.log(
-          "Received foreground push notification:",
-          notification.request.content.title
-        );
+      Notifications?.addNotificationReceivedListener?.((notification: any) => {
+        // Foreground notification received — log in DEV only (console.log is
+        // expensive in production: it serialises the string on the JS thread).
+        if (__DEV__) {
+          console.log(
+            "[Push] Foreground notification:",
+            notification.request.content.title
+          );
+        }
       });
 
     return () => {
-      subscription.remove();
-      foregroundSubscription.remove();
+      subscription?.remove();
+      foregroundSubscription?.remove();
     };
   }, [router, fontsLoaded, storeHydrated]);
 
@@ -550,8 +509,8 @@ function RootLayoutComponent() {
           <TouchableOpacity
             style={styles.suspendedLogoutBtn}
             onPress={async () => {
-              const { signOut } = require("firebase/auth");
-              const { auth } = require("@/config/firebase");
+              // Use static imports (declared at top of file) — avoids blocking
+              // the JS thread with a synchronous require() inside an event handler.
               try {
                 await signOut(auth);
               } catch (e) {}
@@ -569,11 +528,8 @@ function RootLayoutComponent() {
 
   return (
     <View style={{ flex: 1 }} {...edgeSwipePanResponder.panHandlers}>
-      <ExpoStatusBar
-        style={isDark ? "light" : "dark"}
-        translucent={true}
-        backgroundColor="transparent"
-      />
+      <ExpoStatusBar style={isDark ? "light" : "dark"} />
+
       <Tabs
         tabBar={(props) => (
           <CustomTabBar {...props} tabBarTranslateY={tabBarTranslateY} />
@@ -583,24 +539,19 @@ function RootLayoutComponent() {
           headerShown: false,
           tabBarStyle: {
             backgroundColor: isDark
-              ? "rgba(15, 23, 42, 0.75)"
-              : "rgba(255, 255, 255, 0.75)",
+              ? "rgba(15, 23, 42, 0.9)"
+              : "rgba(255, 255, 255, 0.9)",
             position: "absolute",
-            bottom:
-              Platform.OS === "ios"
-                ? Math.max(24, insets.bottom + 8)
-                : Math.max(24, insets.bottom + 16),
-            left: 16,
-            right: 16,
-            borderRadius: 36,
-            borderTopWidth: 0,
-            elevation: 15,
-            boxShadow:
-              Platform.OS === "web" ? `${0}px ${8}px ${16}px #000` : undefined,
-
-            height: 64,
-            paddingBottom: 0,
-            paddingTop: 0,
+            bottom: 0,
+            left: 0,
+            right: 0,
+            borderTopWidth: 1,
+            borderTopColor: isDark ? "rgba(255,255,255,0.1)" : "rgba(0,0,0,0.05)",
+            elevation: 0, // removed elevation since it's flush at the bottom
+            boxShadow: 'none',
+            height: 60 + insets.bottom,
+            paddingBottom: insets.bottom,
+            paddingTop: 8,
           },
           tabBarLabelStyle: {
             fontSize: 11,
diff --git a/src/app/(tabs)/community.tsx b/src/app/(tabs)/community.tsx
index 96b7ce9..3997c68 100644
--- a/src/app/(tabs)/community.tsx
+++ b/src/app/(tabs)/community.tsx
@@ -18,7 +18,8 @@ import {
   ScrollView,
   TouchableWithoutFeedback,
   Animated,
-  BackHandler
+  BackHandler,
+  StatusBar
 } from 'react-native';
 import { TextInput } from '@/components/ui/TextInput';
 import { uploadToCloudinary } from '@/utils/cloudinary';
@@ -970,7 +971,7 @@ export default function CommunityScreen() {
     const displayRole = isCurrentUser && user ? user.role : item.senderRole;
     const displayAdminRole = isCurrentUser && user ? user.adminRole : item.senderAdminRole;
 
-    const isSuperAdmin = item.senderUid === 'Zdxi8kTc2kcs1cOPxWS81PTVmco2' || item.senderUid === 'DdP2c855PSRUJwhmN9rvbkYBraP2' || displayAdminRole === 'SUPER_ADMIN' || displayRole === 'SUPER_ADMIN';
+    const isSuperAdmin = item.senderUid === 'Zdxi8kTc2kcs1cOPxWS81PTVmco2' || item.senderUid === 'DdP2c855PSRUJwhmN9rvbkYBraP2' || displayAdminRole === 'SUPER_ADMIN';
     const isFaculty = displayRole === 'Faculty';
     const isAdmin = displayRole === 'Admin' || displayAdminRole === 'SUPER_ADMIN';
 
@@ -1415,7 +1416,12 @@ export default function CommunityScreen() {
 
   return (
     <View style={[styles.rootContainer, { backgroundColor: theme.background }]}>
-      <View style={{ height: insets.top, backgroundColor: theme.backgroundElement }} />
+      <StatusBar
+        backgroundColor={theme.backgroundElement}
+        barStyle={theme.isDark ? 'light-content' : 'dark-content'}
+        translucent={true}
+      />
+      <View style={{ height: insets.top, backgroundColor: theme.backgroundElement, zIndex: 101 }} />
       
       {!activeRoomId ? (
         <View style={{ flex: 1 }}>
diff --git a/src/app/(tabs)/index.tsx b/src/app/(tabs)/index.tsx
index 2334ab7..f1a7ff4 100644
--- a/src/app/(tabs)/index.tsx
+++ b/src/app/(tabs)/index.tsx
@@ -1491,7 +1491,7 @@ export default function HomeFeedScreen() {
         <View style={{ height: insets.top, backgroundColor: theme.backgroundElement, zIndex: 101 }} />
 
         {/* 1. Facebook-style Premium Feed Header */}
-        <View style={{ zIndex: 100, backgroundColor: theme.background }}>
+        <View style={{ zIndex: 100, backgroundColor: theme.backgroundElement }}>
         <View style={[
           styles.header, 
           { 
@@ -1570,13 +1570,14 @@ export default function HomeFeedScreen() {
         ) : (
           <AnimatedFlashList
             ref={feedListRef}
-            onScroll={(event: any) => {
-              scrollY.setValue(event.nativeEvent.contentOffset.y);
-            }}
+            onScroll={Animated.event(
+              [{ nativeEvent: { contentOffset: { y: scrollY } } }],
+              { useNativeDriver: true }
+            )}
             scrollEventThrottle={16}
             data={filteredPosts}
             extraData={{ user, bookmarkedPostIds, connections }}
-            estimatedItemSize={380}
+            estimatedItemSize={500}
             drawDistance={Platform.OS === 'android' ? 2000 : 1500}
             refreshControl={
               <RefreshControl
diff --git a/src/app/(tabs)/notice.tsx b/src/app/(tabs)/notice.tsx
index 1267beb..dc8c789 100644
--- a/src/app/(tabs)/notice.tsx
+++ b/src/app/(tabs)/notice.tsx
@@ -375,7 +375,7 @@ export default function NoticesHubScreen() {
       />
       <View style={{ height: insets.top, backgroundColor: theme.backgroundElement, zIndex: 101 }} />
       {/* 1. LinkedIn-style Global Header with App Branding */}
-      <View style={{ zIndex: 100, backgroundColor: theme.background }}>
+      <View style={{ zIndex: 100, backgroundColor: theme.backgroundElement }}>
       <View style={[styles.header, { backgroundColor: theme.backgroundElement, height: 60, borderBottomColor: theme.cardBorder }]}>
         <View style={styles.headerTitleCol}>
           <Text style={[styles.headerTitle, { color: theme.text }]}>Notice Board</Text>
diff --git a/src/components/PostCard.tsx b/src/components/PostCard.tsx
index dbeb889..8b9dce6 100644
--- a/src/components/PostCard.tsx
+++ b/src/components/PostCard.tsx
@@ -432,12 +432,14 @@ function PostCardInternal({
         >
           <Ionicons
             name={(user && user.role !== 'Guest' && item.isClapped) ? 'heart' : 'heart-outline'}
-            size={18}
+            size={22}
             color={(user && user.role !== 'Guest' && item.isClapped) ? '#EF4444' : theme.textSecondary}
           />
-          <Text style={[styles.actionText, { color: theme.textSecondary }, (user && user.role !== 'Guest' && item.isClapped) && styles.clappedText]}>
-            {item.claps} Hearts
-          </Text>
+          {item.claps > 0 && (
+            <Text style={[styles.actionText, { color: theme.textSecondary, marginLeft: 4 }, (user && user.role !== 'Guest' && item.isClapped) && styles.clappedText]}>
+              {item.claps}
+            </Text>
+          )}
         </TouchableOpacity>
 
         <TouchableOpacity
@@ -445,27 +447,28 @@ function PostCardInternal({
           onPress={() => onCommentPress(item)}
           activeOpacity={0.7}
         >
-          <Ionicons name="chatbubble-outline" size={18} color={theme.textSecondary} />
-          <Text style={[styles.actionText, { color: theme.textSecondary }]}>{item.commentsCount} Comments</Text>
+          <Ionicons name="chatbubble-outline" size={20} color={theme.textSecondary} />
+          {item.commentsCount > 0 && (
+            <Text style={[styles.actionText, { color: theme.textSecondary, marginLeft: 4 }]}>
+              {item.commentsCount}
+            </Text>
+          )}
         </TouchableOpacity>
 
         <TouchableOpacity
           style={styles.actionBtn}
-          onPress={() => onSharePress ? onSharePress() : alert('Link copied to clipboard!')}
+          onPress={() => onForwardPress && onForwardPress()}
           activeOpacity={0.7}
         >
-          <Ionicons name="share-social-outline" size={18} color={theme.textSecondary} />
-          <Text style={[styles.actionText, { color: theme.textSecondary }]}>Share</Text>
+          <Ionicons name="arrow-redo-outline" size={22} color={theme.textSecondary} />
         </TouchableOpacity>
 
-        {/* Forward button */}
         <TouchableOpacity
           style={styles.actionBtn}
-          onPress={() => onForwardPress?.()}
+          onPress={() => onSharePress ? onSharePress() : alert('Link copied to clipboard!')}
           activeOpacity={0.7}
         >
-          <Ionicons name="arrow-redo-outline" size={18} color={theme.textSecondary} />
-          <Text style={[styles.actionText, { color: theme.textSecondary }]}>Forward</Text>
+          <Ionicons name="share-social-outline" size={22} color={theme.textSecondary} />
         </TouchableOpacity>
       </View>
 
diff --git a/src/components/drawer/CustomDrawer.tsx b/src/components/drawer/CustomDrawer.tsx
index abca83e..741388d 100644
--- a/src/components/drawer/CustomDrawer.tsx
+++ b/src/components/drawer/CustomDrawer.tsx
@@ -212,7 +212,7 @@ export const CustomDrawer = forwardRef<CustomDrawerRef, CustomDrawerProps>(({
   const panResponder = useRef(
     PanResponder.create({
       onStartShouldSetPanResponder: () => false,
-      onMoveShouldSetPanResponder: (evt, gestureState) => {
+      onMoveShouldSetPanResponderCapture: (evt, gestureState) => {
         const startX = evt.nativeEvent.pageX;
         if (progressVal.current === 0) {
           if (startX > 45) return false;
@@ -354,15 +354,14 @@ export const CustomDrawer = forwardRef<CustomDrawerRef, CustomDrawerProps>(({
         {/* Main Background Screen Content wrapped in transition */}
         <Animated.View style={[styles.mainScreenContainer, { backgroundColor: theme.background }, mainScreenStyle]}>
           {children}
-          
-          {/* Transparent backdrop overlay shade placed inside mainScreenContainer to resolve CSS stacking context and block background interactions */}
-          {isOpenJS && (
-            <Animated.View style={[styles.overlayShadow, overlayStyle]}>
-              <Pressable style={styles.overlayPressable} onPress={closeDrawer} />
-            </Animated.View>
-          )}
         </Animated.View>
 
+        {isOpenJS && (
+          <Animated.View style={[StyleSheet.absoluteFillObject, { zIndex: 10 }, overlayStyle]}>
+            <TouchableOpacity activeOpacity={1} style={{ flex: 1 }} onPress={closeDrawer} />
+          </Animated.View>
+        )}
+
         {/* Drawer Panel Surface rendered AFTER mainScreenContainer so it is ALWAYS on top in the DOM stacking hierarchy */}
         <Animated.View 
           pointerEvents={isOpenJS ? "box-none" : "none"}

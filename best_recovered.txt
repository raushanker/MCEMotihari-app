"import 'react-native-gesture-handler';\nimport React, { useState, useEffect } from 'react';\nimport { \n  StyleSheet, View, Text, TouchableOpacity, Modal, TextInput, \n  ScrollView, KeyboardAvoidingView, Platform, Dimensions, Switch,\n  ActivityIndicator\n} from 'react-native';\nimport { DarkTheme, DefaultTheme, ThemeProvider } from '@react-navigation/native';\nimport { useColorScheme } from 'react-native';\nimport { Tabs, useRouter } from 'expo-router';\nimport { Ionicons } from '@expo/vector-icons';\nimport { useAppStore } from '@/store/useAppStore';\n\nconst { height, width } = Dimensions.get('window');\n\nconst CATEGORIES = ['General', 'Clubs', 'Hostels', 'Placement', 'Sports', 'Alumni'];\n\nexport default function TabLayout() {\n  const colorScheme = useColorScheme();\n  const theme = colorScheme === 'dark' ? 'dark' : 'light';\n  const router = useRouter();\n\n  // Zustand Store integrations\n  const { \n    user, initStore,\n    isCreatePostVisible, setCreatePostVisible, createPost\n  } = useAppStore();\n\n  // Load store resources on mount\n  useEffect(() => {\n    initStore();\n  }, []);\n\n  // Post Creator States\n  const [postTitle, setPostTitle] = useState('');\n  const [postContent, setPostContent] = useState('');\n  const [postCategory, setPostCategory] = useState<any>('General');\n  const [isAnonymous, setIsAnonymous] = useState(false);\n  const [postLink, setPostLink] = useState('');\n  const [isPollMode, setIsPollMode] = useState(false);\n  const [pollOptions, setPollOptions] = useState<string[]>(['', '']);\n  const [isSubmitting, setIsSubmitting] = useState(false);\n\n  const handleOpenFAB = () => {\n    if (!user) {\n      router.push('/login');\n      return;\n    }\n    setCreatePostVisible(true);\n  };\n\n  const handlePublishPost = async () => {\n    if (!user || !postTitle.trim() || !postContent.trim()) {\n      alert('Please fill out the headline and content!');\n      return;\n    }\n\n    setIsSubmitting(true);\n    try {\n      await createPost({\n        authorName: user.name,\n     
<truncated 18890 bytes>